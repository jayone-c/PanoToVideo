# 全景图转短视频工具 开发规划

- 状态:规划文档,待确认后进入执行
- 上游:[全景图转短视频工具-PRD.md](全景图转短视频工具-PRD.md)
- 目标平台:Windows 10/11 x64
- 编写日期:2026-07-21

---

## 0. 环境核验结论(已实测)

规划建立在以下已实测的环境事实之上:

| 项 | 结论 | 影响 |
|---|---|---|
| FFmpeg 8.1.1 | `v360`、`h264_nvenc`、`hevc_nvenc`、`h264_amf`、`hevc_amf`、`h264_qsv`、`hevc_qsv`、`mediafoundation` 全部在线 | CPU 回退、编码兜底、几何参考均可用 |
| ffprobe | 可用 | 验收阶段做容器/流校验 |
| GPU | RTX 4090 D 24GB(NVENC 在线)+ AMD Radeon 核显 + 2 个虚拟显示适配器 | 设备选择必须排除核显与虚拟显示适配器,默认命中 4090 D |
| .NET 8 SDK 8.0.204 | `Microsoft.NET.Sdk.WindowsDesktop` 在位,WPF 可用 | UI 技术栈无需额外安装 |
| VS 2022 Build Tools | 已装于 `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`,C++ 工具链可用(`cl` 不在 PATH,需 vcvars/CMake) | 解锁 PRD #7 预留的 C++/WinRT 下沉路径 |
| Windows SDK 10.0.26100 | `fxc.exe` 在 `bin\10.0.26100.0\x64\` | HLSL 离线编译,可在构建期把 shader 预编译进资源 |
| Python 3.12 | 可用 | 装 py360convert 做几何参考基准 |
| ERP 测试图 | 仅 `D:/work/720download/_diagnose_justeasy_faces/` 下 3 张 8192×4096 | PRD Testing 要求的 6000×3000、7680×3840、16000×8000 **不存在**,需补齐(见 §9) |

**与 PRD 的差异**:
1. 本机存在核显与虚拟显示适配器,PRD 假设的"独显直接命中"不成立,设备选择逻辑必须显式处理。
2. 多尺寸测试图缺失,验收阶段前必须补齐。
3. C++ Build Tools 已装,下沉路径无环境成本。

---

## 1. 技术栈与架构决策(渐进路线)

### 1.1 路线选定

采用**渐进路线**:
- UI 层 → WPF(.NET 8)
- GPU 投影 → C# + Vortice.Windows(D3D11 + HLSL)
- 硬件编码 → **先用 C# 探针验证 Vortice 的 Media Foundation 绑定能否吃 D3D11 交互纹理(零拷贝)**;若不能,仅把"MF 编码器"一段下沉为 C++/WinRT 本地 DLL,投影仍在 C# 侧。
- CPU 回退 → FFmpeg 子进程(`v360` + 软/硬编码)

**为什么不是直接全 C++/WinRT**:工程重、迭代慢、跨边界调试成本高,且 PRD 要求第一阶段就要冒烟验证,先用 C# 验证最快。
**为什么不是全 C# + Vortice**:零拷贝编码(D3D11 纹理 → IMFTransform)依赖 `IMFDXGIDeviceManager` / `MFCreateDXGISurfaceBuffer` 这类 MF COM 接口,Vortice 是否完整暴露未经验证;若不通,会被迫 GPU→CPU 回读,撞不到 60 FPS 验收线。
**渐进路线的本质**:把"零拷贝是否可行"这个最大不确定性,放在阶段 0 用最小代价探明,再决定编码段是否下沉。

### 1.2 决策门(阶段 0 必须通过)

阶段 0 的 C# MF 探针要回答:

1. 能否在 C# 中通过 `MFCreateDXGIDeviceManager` 把 D3D11 设备挂给 MF H.264 编码器?
2. 能否用 `MFCreateDXGISurfaceBuffer` 把 D3D11 渲染目标纹理零拷贝喂给编码器输入样本?
3. 编码器实际接受的 GPU 输入是否为 NV12;若渲染目标为 BGRA/RGBA,RGB→NV12、sRGB→Rec.709/limited-range 转换能否全程在同一 GPU 完成?
4. 渲染 D3D11 设备与硬件编码器是否绑定同一个 DXGI Adapter LUID,而非跨 GPU 传输?
5. 端到端(8192×4096 → 1080×1920、3 秒、60FPS、H.264)投影/颜色转换/编码/总耗时各多少?是否 ≥60 输出 FPS?

- **三项全通** → 全程 C#,编码不下沉。
- **投影通、零拷贝编码不通** → 编码段下沉 C++/WinRT DLL,投影留 C#。DLL 通过 P/Invoke 暴露 `Encode_Init / Encode_SubmitTexture / Encode_Finish`。
- **探针无法在 C# 完成投影** → 工作器整体下沉 C++/WinRT(回退到 PRD #7 的兜底)。

> 该决策门的结果必须写入 `docs/adr/0001-技术栈与零拷贝路径.md`,作为后续所有阶段的执行依据。

### 1.3 数据流(目标态,零拷贝)

```
ERP JPG
  │  (一次性上传,D3D11 2D 纹理 + ShaderResourceView)
  ▼
┌─────────────── GPU(D3D11 设备,命中 RTX 4090 D)───────────────┐
│  逐帧 BGRA RenderTargetView(1080×1920)                       │
│    └─ HLSL pixel shader:equirect→perspective                  │
│         Yaw = 起始 + 方向 × 旋转度 × 帧序/(总帧数-1)          │
│         Pitch = 用户设置(固定)                                │
│         经度环绕采样(Wrap + 双线性)                           │
│  GPU 色彩转换(BGRA/sRGB → NV12/Rec.709 limited)               │
│  NV12 纹理 ──零拷贝──► MF H.264 编码器(IMFDXGIBuffer)        │
│                              │                                │
│                              ▼                                │
│                    H.264 码流 ──► MF MP4 muxer               │
└────────────────────────────┬─────────────────────────────────┘
                             ▼
       同目录临时文件 {文件名}.{guid}.tmp.mp4 ──成功──► 原子重命名 *.mp4
```

关键不变式:**渲染、颜色转换与编码纹理共享同一 D3D11 设备和 DXGI Adapter LUID,全程不经 CPU 回读**。任何"下载到 staging 纹理再上传"或跨适配器复制的实现都视为破坏 PRD #1,必须在阶段 0 暴露。

### 1.4 投影数学(PRD #3 落地)

对输出像素 `(u, v)`:
1. 由 FOV 与输出宽高比推导透视相机射线方向 `d`(相机本地坐标)。
2. 普通导出使用 `Yaw(t) = startYaw + direction × rotationDeg × t/(N-1)`；无缝循环且旋转度数为 360° 整数倍时使用 `Yaw(t) = startYaw + direction × rotationDeg × t/N`，避免循环边界重复首帧导致一帧停顿。`t` 为帧序,`N = 时长×FPS`。
3. 旋转矩阵 `R = RotY(Yaw) × RotX(Pitch)` 应用到 `d`,得世界射线。
4. 转球面坐标 `(θ_lon, φ_lat)`,映射到 ERP 纹理坐标:`u_tex = θ_lon/2π + 0.5`,`v_tex = φ_lat/π + 0.5`。
5. 双线性采样,经度 `0/360` 环绕(`AddressU = Wrap`)。

FOV 语义修正(PRD Further Notes):界面 FOV 一律为**水平 FOV**,垂直 FOV 由输出宽高比推导,二者不共存为独立输入。

### 1.5 小行星开场(PRD #4 落地)

- 独立投影过渡,不进通用剪辑系统。
- 启用时前 0.8 秒(固定)从小行星投影(底部极点向下俯视)过渡到用户透视投影与 FOV,之后再按正常旋转导出。
- 小行星是独立球面投影公式,不是把普通透视 FOV 设为 180°；普通透视 FOV 必须始终小于 180°，以避免数学奇点。
- 实现上复用同一个 D3D11 管线,通过 uniform 控制投影模式与参数插值,不引入第二套渲染路径。

---

## 2. 工程结构

```
720-to-video/                         仓库根(git init)
├── 全景图转短视频工具-PRD.md           已有
├── 全景图转短视频工具-开发规划.md       本文
├── CONTEXT.md                         单上下文总览(按全局 CLAUDE.md 约定)
├── docs/
│   └── adr/
│       ├── 0001-技术栈与零拷贝路径.md   阶段0决策门产物
│       └── 0002-设备选择与回退策略.md
├── .scratch/                          本地 issue tracker(按全局 CLAUDE.md)
├── src/
│   ├── PanoToVideo.sln
│   ├── PanoToVideo.App/              WPF 主程序(.NET 8,net8.0-windows)
│   │   ├── ViewModels/               参数面板、队列、详情 VM
│   │   ├── Views/                    顶部/左/中/右/底部五区
│   │   └── (设置写入 %LocalAppData%\PanoToVideo\AppSettings.json)
│   ├── PanoToVideo.Core/             领域层(纯 C#,无 UI/GPU 依赖)
│   │   ├── Parameters/               单图参数模型 + 校验(2:1±1%、偶数尺寸、范围)
│   │   ├── Queue/                    任务项、状态机、串行调度器
│   │   ├── Naming/                   输出命名 + 重名递增
│   │   └── Logging/                  任务日志模型
│   ├── PanoToVideo.Render/           GPU 渲染+编码工作器
│   │   ├── D3D11/                    Vortice 投影、shader、纹理管理
│   │   ├── Encoding/                 MF 编码(C# 探针 → 视决策门下沉)
│   │   ├── DeviceProbe/              DXGI 适配器枚举 + MF 编码器探测
│   │   └── Fallback/                 FFmpeg CPU 回退链路
│   ├── PanoToVideo.Native/           可选:C++/WinRT 编码 DLL(按决策门触发)
│   │   ├── CMakeLists.txt
│   │   └── src/encode_bridge.cpp
│   └── PanoToVideo.Tests/            xUnit
│       ├── Geometry/                 与 py360convert/v360 逐帧对比
│       ├── Validation/               校验规则
│       └── Queue/                    状态机
├── tools/
│   └── gen-test-images/              生成缺失尺寸 ERP 测试图
└── tests/
    ├── fixtures/                     ERP 测试图(含补齐的多尺寸)
    └── reference/                    py360convert/v360 参考帧
```

**依赖方向**:`App → Core ← Render`;`Core` 不引用 `Render`/`App`,保证领域逻辑可单测。
**原生 DLL**:仅当决策门判定下沉时创建 `PanoToVideo.Native`,CMake + VS 2022 工具链构建,产物随应用打包。

---

## 3. 分阶段计划

对齐 PRD `Agent Delivery Order`,在前面插入**阶段 0 决策门**。每阶段给目标、任务、产出、验收门槛。

### 阶段 0:工程地基与零拷贝决策门

**目标**:用最小代价探明 §1.2 的三个问题,锁定后续技术路径。

**任务**
1. `git init`,建立 `CONTEXT.md`、`docs/adr/`、`.scratch/`。
2. 建解决方案与四个项目骨架(App/Core/Render/Tests),可编译空壳。
3. `PanoToVideo.Render` 接入 Vortice.Windows,完成 D3D11 设备初始化 + 一张 ERP 图上传为 SRV。
4. 写 HLSL equirect→perspective shader(`fxc` 预编译为 `.cso` 嵌入资源),渲染单帧到 1080×1920 RenderTarget。
5. MF 探针:尝试 `MFCreateDXGIDeviceManager` + H.264 硬件编码器 + `MFCreateDXGISurfaceBuffer` 零拷贝输入,记录每一步可用性、输入像素格式、渲染/编码 Adapter LUID 与颜色范围。
6. 验证 GPU 内 BGRA/sRGB → NV12/Rec.709 limited 转换;不接受由 Sink Writer 或编码器隐式触发的 CPU 色彩转换。
7. 端到端冒烟:8192×4096 ERP → 3 秒、1080×1920、60FPS、H.264 MP4,分别测投影、颜色转换、编码、总耗时。
8. 写 `docs/adr/0001`,记录决策门结论与选定路径。

**产出**:冒烟 MP4 + 性能日志 + ADR 0001。

**验收门槛**
- 冒烟可播放,ffprobe 校验为 H.264、1080×1920、60FPS、3 秒、无音频。
- 性能日志明确区分"投影设备 LUID / 编码设备 LUID / 输入像素格式 / 颜色转换设备 / 是否零拷贝"。
- 给出 60 FPS 可达性的初判:若冒烟已 <30 秒,后续乐观;若 >60 秒,必须在日志定位瓶颈(投影/回读/编码/会话启动)再继续。

**风险**:Vortice MF 绑定不全 → 触发下沉;性能未达预期 → 先做瓶颈定位而非盲目优化。

---

### 阶段 1:单图端到端(PRD Delivery Order #1+#2)

**目标**:单图从拖入到可播放 MP4,全参数可控。

**任务**
1. ERP 校验:宽高比 `2.0 ± 1%`,损坏文件读取失败处理,显示实际尺寸与比例。
2. 单图参数面板:视频长度、旋转度数(允许 >360°)、FPS(24/25/30/50/60)、FOV(30°–110°)、宽高(偶数,预设+自定义)、俯仰角(-85°..85°)、顺/逆时针、小行星开关、记住配置、完成后打开、CPU 核心(仅回退可编辑)。
3. 设备探测:`DeviceProbe` 枚举 DXGI 适配器,排除 `DXGI_ADAPTER_FLAG_SOFTWARE` 软件适配器；对每个候选适配器用其 LUID 枚举 MF 硬件编码器并完成一次纹理输入探测。不得仅以 `DedicatedVideoMemory=0` 判定虚拟适配器，因为核显也可能为 0；默认优先命中 4090 D，并允许用户在高级设置中手动选择。界面显示实际投影设备、编码器和两者 LUID。
4. 单图导出全链路:参数 → 纹理上传 → 逐帧投影 → 零拷贝编码 → MP4(faststart)→ 临时文件 + 原子重命名。
5. 几何对照测试:Yaw {0,90,180,270}° × Pitch {-30,0,30}° × FOV {45,75,100}° 的**编码前 GPU 原始帧**与 py360convert、`v360` 参考图逐像素差异比对,接缝处不得黑边/断层/翻转；编码后的 MP4 另做播放与较宽松的视觉质量验证。
6. 360° 整圈首尾视角一致性验证(允许编码压缩差异,不允许几何跳变)。

**产出**:可单图导出的应用 + 几何对照测试套件。

**验收门槛**
- 单图全参数组合可导出,ffprobe 通过。
- 编码前几何对照差异在经校准容差内(建议 PSNR ≥40dB 或 SSIM ≥0.99,接缝单独检查)；该阈值不得直接用于有损 H.264/H.265 成片。
- 界面显示的设备/编码器与日志一致,不得只显示"GPU 加速"文案。

---

### 阶段 2:批量队列(PRD Delivery Order #3)

**目标**:100+ 图批量、串行、可恢复。

**任务**
1. 队列项模型:缩略图、源文件名、尺寸、状态、进度、实际 FPS、ETA、输出路径、错误原因。
2. 状态机:`待校验 → 待处理 → 处理中 → 完成`;异常→`失败`(可重试);取消→`已取消`。零拷贝任务中投影与编码按帧流水并行，`处理中`内分别上报投影 FPS、颜色转换 FPS、编码 FPS 与写入进度，而非伪造串行“渲染中→编码中”。
3. 串行调度器:一次一个 GPU 任务(防显存/编码会话争抢);CPU 回退任务亦串行。
4. 命名:`{原文件名}_{宽}x{高}_{时长}s_{旋转度}deg.mp4`,重名递增不覆盖;默认输出到所选目录下 `exports/`。
5. 导出前预检:总帧数、按质量预设码率计算的保守预估体积(额外预留 15%)、可用磁盘空间,不足不开始。
6. 进度:队列总进度 + 当前任务帧进度 + 已耗时 + ETA + 投影/编码实际 FPS。
7. “暂停队列”定义为当前任务完成后停止启动下一项；“取消当前任务”才会中断当前编码。支持重试失败项;失败单项不阻塞队列。
8. 日志:每任务记录参数、设备、编码器、耗时、平均 FPS、输出文件、失败详情。
9. 临时文件清理:失败/取消时清理 `.tmp`,不删源图。

**产出**:批量队列完整可用。

**验收门槛**
- 100 项批量跑通:顺序正确、重名处理正确、单项失败后队列继续、失败可重试、取消清理临时文件、完成后打开目录。
- 日志能证明每任务的实际设备与编码器。

---

### 阶段 3:体验闭环(PRD Delivery Order #4)

**目标**:补齐 PRD 截图中所有交互。

**任务**
1. 配置记忆:最近一次有效配置持久化到 `AppSettings.json`。
2. 完成后打开:单图→打开该 MP4;批量→打开输出目录。
3. 输出预设:"兼容优先"(H.264)/"体积优先"(H.265);所选编码器不支持时提示并退回 H.264。
4. 小行星开场实现(§1.5):关闭时第 0 帧即透视;启用时 0.8 秒过渡后无突跳进入旋转。
5. 无缝循环提示:旋转度数非 360° 整数倍时提示"非无缝循环"。

**产出**:全交互闭环。

**验收门槛**
- 小行星开/关行为符合预期(第 0 帧/0.8 秒后无突跳)。
- H.265 预设在 NVENC 不可用时正确退回 H.264 并提示。
- 配置重启后复用。

---

### 阶段 4:验收与发布(PRD Delivery Order #5)

**目标**:达标后打包发布,不达标不标记完成。

**任务**
1. 几何对照全量(Yaw/Pitch/FOV 矩阵 + 接缝专项)。
2. 播放兼容性:Windows 默认播放器 + 主流短视频剪辑软件。
3. 错误恢复:非 2:1、损坏图、无写权限、磁盘不足、GPU 初始化失败、硬件编码不可用,逐一验证文案与回退。
4. RTX 4090 D 性能验收:标准基准任务(7680×3840 → 1080×1920、30 秒、60FPS、360°、75° FOV、H.264)端到端 ≥60 输出 FPS,日志证明投影与编码实际设备。
5. ffprobe 校验全部输出:视频流、分辨率、帧率、时长、无音频流。
6. 打包:单目录可运行,附 FFmpeg LGPL/GPL 许可文件。

**验收门槛**(对齐 PRD 成功标准)
1. 默认 GPU 投影+硬件编码,界面显示实际设备/编码器。
2. RTX 4090 D 基准 ≥60 FPS(纯渲染 ≤30 秒),日志可证。**未达标不得标记完成。**
3. ≥100 图批量,单项失败不阻塞,可重试。
4. CPU/GPU 不足时解释原因并提供 CPU 回退,不伪称硬件加速。
5. 输出可在 Windows 默认播放器与主流剪辑软件打开。

---

## 4. 关键风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| Vortice MF 绑定不全,零拷贝编码做不出 | 撞不到 60 FPS | 阶段 0 决策门先探明;不通则编码段下沉 C++/WinRT(已装 BuildTools) |
| 性能未达 60 FPS | 验收失败 | 阶段 0 即测;瓶颈定位按"投影/回读/编码/会话启动"分类,不盲目优化 |
| 接缝几何错误 | 输出肉眼可见瑕疵 | 阶段 1 几何对照专项,接缝单独检查 |
| 多 GPU 误选核显/虚拟适配器 | 速度不达标且不报错 | 排除 DXGI 软件适配器；按 Adapter LUID 枚举并实测 MF 硬件编码器；允许手动选卡，渲染与编码必须同 LUID |
| 超大图显存 | 16000×8000 纹理 ≈512MB(R8G8B8A8) | 纹理上传一次、全程驻留;24GB 足够,但需限制并发数=1 |
| `cl` 不在 PATH | C++ 编译失败 | 用 CMake + VS 2022 生成器或 vcvars 环境,不依赖全局 PATH |
| 缺失多尺寸测试图 | 验收场景不全 | §9 补齐脚本,阶段 4 前完成 |
| FFmpeg 许可 | 发布合规 | 核对实际构建的 LGPL/GPL 组件,随包附许可 |

---

## 5. 测试策略(对齐 PRD Testing Decisions)

**原则**:只验用户可见结果与设备行为,不绑死类/着色器/控件结构;所有 GPU 结果与可信 CPU 参考做图像差异比对。

- **几何参考**:`py360convert`(Python/NumPy)与 `v360`(FFmpeg)双参考;比较编码前 GPU 原始帧而非有损成片。
- **分层矩阵**:四种输入尺寸 × Yaw/Pitch/FOV 矩阵只跑单帧几何测试；输出尺寸/FPS/旋转/小行星组合只选代表性完整视频冒烟用例，避免把 96 个组合都跑成长视频。
- **性能基准**:RTX 4090 D 实测端到端 FPS、CPU/GPU 利用率、显存峰值、输出耗时;日志证明投影与编码设备。
- **验收**:输出 MP4 由 ffprobe 校验为视频流、正确分辨率/帧率/时长、无音频流。

---

## 6. 依赖与许可

| 组件 | 用途 | 许可 | 备注 |
|---|---|---|---|
| Vortice.Windows | D3D11/DXGI/MF 绑定 | MIT | 决策门验证 MF 完整性 |
| FFmpeg | CPU 回退、封装、几何参考、验收 | LGPL/GPL | 发布核对实际构建组件并附许可 |
| py360convert | 几何参考基准(仅测试) | MIT | 不进生产 |
| C++/WinRT(若下沉) | 原生编码 DLL | MIT | 仅决策门触发时引入 |
| fxc | HLSL 离线编译 | Windows SDK 随附 | 构建期预编译 shader |

**禁止**:直接复制 hvs_vr_encoding 代码(仓库未明确可复用许可),仅作算法/性能调研参考(对齐 PRD #7)。

---

## 7. 设备选择与回退策略细则

**选择优先级**(PRD #2):
1. 枚举 DXGI 适配器,先过滤 `DXGI_ADAPTER_FLAG_SOFTWARE`；不得仅凭 `DedicatedVideoMemory == 0` 排除设备。
2. 对候选适配器创建 D3D11 设备，并以该 Adapter LUID 枚举 MF H.264 硬件编码器及完成 NV12 纹理输入探测。
3. 命中"同 LUID 的 GPU 投影 + 硬件编码"者，优先独显，再按显存与探针性能排序；高级设置允许用户手动指定。
4. 无独显则使用通过同一探针的核显;再无则 CPU 回退。

**回退触发**(PRD #5):
- GPU 初始化失败、硬件编码器不可用 → FFmpeg `v360` 投影 + 可用硬件编码(`h264_nvenc`/`h264_amf`/`h264_qsv`)或软件 `libx264`。
- **回退必须在 UI 与日志显式标注"CPU 回退",不得悄悄走 CPU。**
- GPU 路径中"CPU 核心"参数不生效;仅在 CPU 回退时影响 `libx264 -threads`。

---

## 8. 输出契约

- 容器:MP4;H.264(`yuv420p`、Rec.709 limited、无音频)为默认。`faststart`不是 Media Foundation 的默认承诺；若产品仍要求，应在阶段 0 验证输出 moov 位置，必要时在完成编码后做一次无重编码 remux。
- 质量预设:"兼容优先"=H.264 16 Mbps(1080×1920、60 FPS基准;其他尺寸按像素数与帧率换算);"体积优先"=H.265 8 Mbps(同基准)。不支持时退回 H.264 并提示。预检按目标码率 × 时长 × 1.15 估算空间。
- 命名:`exports/{原文件名}_{宽}x{高}_{时长}s_{旋转度}deg.mp4`,重名追加递增序号不覆盖。
- 原子写:同一输出目录内先写 `{文件名}.{guid}.tmp.mp4`,编码成功后原子重命名;失败/取消清理临时文件,不删源图。
- 输入契约:标准 `*_equirectangular_<w>x<h>.jpg`(全景下载器产物),宽高比 `2.0 ± 1%`。

---

## 9. 待补齐项(执行前必须完成)

1. **多尺寸 ERP 测试图**:
   - 6000×3000、7680×3840:用 ffmpeg 从现有 8192×4096 缩放生成(lanczos)。
   - 16000×8000:以合成经纬网/棋盘/经度接缝图生成，用于验证解码、显存和接缝数学；如需评价真实纹理质量，另补真实高分 ERP，不得用放大图替代。脚本置于 `tools/gen-test-images/`。
2. **py360convert 安装**:`pip install py360convert numpy pillow`,生成参考帧脚本置于 `tests/reference/`。
3. **git 仓库初始化**:当前非 git 仓库(PRD 已说明),执行阶段 0 时 `git init` 并建 `.gitignore`。
4. **CPU 回退动态旋转验证**:明确并实现静态 ERP 的逐帧 Yaw 动画方式，并在无 GPU 条件下用 3 秒任务验证方向、时长和帧率；不能只验证 FFmpeg 存在 `v360` 过滤器。

---

## 10. 执行入口

确认本规划后,建议从**阶段 0** 启动,首个里程碑即 §1.2 的零拷贝决策门 + 冒烟性能日志。该里程碑通过前,不进入阶段 1 的 UI 与队列开发(对齐 PRD Further Notes:"不能先做完整界面、最后才验证 GPU 管线")。
