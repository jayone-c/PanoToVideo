# ADR 0001: 技术栈与零拷贝路径

- 状态：**决策门通过（Q1/Q2/Q3），选定全程 C#；Q4/Q5 待基准任务验收**
- 日期：2026-07-21

## 背景
开发规划 §1.2 列出阶段0决策门必须回答的 5 个问题：

1. 能否在 C# 中通过 `MFCreateDXGIDeviceManager` 把 D3D11 设备挂给 MF H.264 编码器？
2. 能否用 `MFCreateDXGISurfaceBuffer` 把 D3D11 渲染目标纹理零拷贝喂给编码器输入样本？
3. 编码器实际接受的 GPU 输入是否为 NV12；RGB->NV12、sRGB->Rec.709/limited-range 转换能否全程在 GPU 完成？
4. 渲染 D3D11 设备与硬件编码器是否绑定同一个 DXGI Adapter LUID？
5. 端到端各阶段耗时与是否 ≥60 输出 FPS？

## 接口可行性探查（已完成）
Vortice.MediaFoundation 3.8.3 **完整暴露**零拷贝编码所需 COM 接口：
`MFCreateDXGIDeviceManager`(3) / `IMFDXGIDeviceManager`(41) / `MFCreateDXGISurfaceBuffer`(3) /
`IMFDXGIBuffer`(18) / `IMFTransform`(86) / `IMFSinkWriter`(71) / `H264`(52)。
-> 纯 C# 路径接口层面无障碍，无需下沉 C++/WinRT。

## 渲染探针结论（已完成，2026-07-21）
**Vortice C# + D3D11 渲染路径完全可用**。GPU shader 与 py360convert、Core EquirectRenderer
双重对照，7 视角 PSNR **49-54dB**（远超 40dB 阈值）。

诊断中发现并修复的两个根因（非 Vortice 绑定问题）：
1. **背面剔除**：大三角形 CCW winding 被默认 `CullMode.Back` 剔除，导致 Draw 静默不画。
   修复：显式 `RasterizerState` 设 `CullMode.None`。
2. **SV_Position 像素中心偏移**：PS 中 SV_Position 是像素中心 (i+0.5)，而 Core/py360convert
   用端点对齐归一化 `i/(W-1)`。直接用 `input.pos.x` 导致整体 0.5 像素偏移（PSNR 19.79dB）。
   修复：shader 内 `px = input.pos.x - 0.5`。

环境验证：D3D11 设备+纹理正常（FFmpeg d3d11va 硬件解码成功）；NVENC 正常
（FFmpeg h264_nvenc 输出成功）。本机 3 个"NVIDIA RTX 4090 D"镜像适配器，需 MF 探测区分（见 ADR 0002）。

## shader 编译策略调整
原计划 fxc 预编译 .cso 嵌入资源（规划 §1.1）。MSBuild 动态添加 EmbeddedResource
时序问题导致无法可靠嵌入（`ManifestResourceName` 在 `PrepareResourceNames` 之后才被设置）。
改为 **Vortice.D3DCompiler 运行时编译**，.hlsl 作为 Content 复制到输出目录。
等价、无外部依赖、无 fxc 路径要求。**注意：HLSL 注释必须用英文**，中文注释经 D3DCompiler
marshal 后乱码会破坏语法。

## 待验证（下一步：零拷贝编码端到端，ADR Q2-Q5）

### Q1 已通过（2026-07-21）
- `MFCreateDXGIDeviceManager` + `manager.ResetDevice(d3d11)` 成功挂 D3D11 设备（Token 验证）
- `MFTEnumEx` 枚举到 3 个 H.264 硬件编码器（NVENC + AMD AMF + Intel QSV，多 GPU 系统正常）
- 实现：`Render/DeviceProbe/MfDeviceProbe.cs`

### Q2-Q5 验证进度
- Q2 ✓已通过（2026-07-21）：端到端零拷贝编码成功
  - NV12 平面纹理 RTV 可创建并渲染（Y plane R8_UNorm + UV plane R8G8_UNorm）。
  - `MFCreateDXGISurfaceBuffer` 零拷贝包裹 NV12 纹理（subresource=0）。
  - `MfH264Encoder`：SinkWriter 挂 IMFDXGIDeviceManager + H.264 输出 + NV12 输入 +
    逐帧 `SubmitFrame` 零拷贝提交 + Finalize 写 MP4。
  - 端到端：30 帧（不同 Yaw）-> NV12 -> 零拷贝编码 -> `smoke_output.mp4`（186825 字节）。
  - ffprobe 校验：H.264、320×320、30/1 FPS、1秒、30 帧、无音频流。**全部符合 PRD 验收要求**。
- Q3 ✓已通过（2026-07-21）：GPU BGRA->NV12/Rec.709 limited 颜色转换，4 色块全精确匹配
  （白 Y=235/Cb=Cr=128；红 Y=63/Cb=102/Cr=240；绿 Y=173/Cb=42/Cr=26；蓝 Y=32/Cb=240/Cr=118）。
  实现：`EquirectPipeline.ConvertBgraToYuv` + `RenderFrameToNv12` + `colorconvert.hlsl`。
- Q4 ✓已通过（2026-07-21，阶段1）：DeviceProbe 真实化
  - `DeviceProbe.Probe()`：对每个非软件 DXGI 适配器创建 D3D11 设备 + 挂 MF + 枚举 H.264 硬件编码器
    + `IMFActivate.ActivateObject<IMFTransform>()` 验证可在该设备激活编码器，填真实 `HasHardwareEncoder`。
  - 验证：4 个合格设备（3 个 NVIDIA 镜像共享 4090D + 1 AMD 核显），WARP 与虚拟显示适配器
    （USB Mobile Monitor / Todesk）被排除（无 MF 硬件编码器）。首选命中真实 4090D（24GB，Luid=0x017837）。
  - 近似：3 个 NVIDIA 镜像适配器共享同一物理 GPU，都能 ActivateObject 通过；真正的"区分物理 GPU"
    需每适配器做实际 NV12 纹理编码探测。当前已满足核心目标（排除虚拟显示适配器 + 命中独显），
    比"仅凭 Description 选卡"可靠。命名见 ADR 0002。
  - 实现：`Render/DeviceProbe/DeviceProbe.cs`（DeviceProbeResult/DeviceEntry）。
- Q5: 端到端 FPS（8192×4096 -> 1080×1920、3秒、60FPS、H.264）≥ 60 输出 FPS。
  当前 320×320×1秒×30帧测试通过，需用 PRD 基准任务测真实性能。

## 决策分支（选定）
- [x] **全程 C#**（渲染 Q3 + 零拷贝编码 Q2 均通过）-- **选定此路径**，编码不下沉 C++/WinRT。
  - 渲染：Vortice.Direct3D11 + HLSL（运行时 D3DCompiler 编译）
  - 颜色转换：GPU shader BGRA->NV12/Rec.709 limited
  - 编码：Vortice.MediaFoundation IMFSinkWriter + MFCreateDXGISurfaceBuffer 零拷贝
- [ ] 编码段下沉 C++/WinRT DLL（否决：Q2 已验证 C# 零拷贝可行）
- [ ] 工作器整体下沉 C++/WinRT（否决）

## 阶段3 扩展实现（2026-07-22）
- **小行星开场（§1.5）**：`asteroid.hlsl` 独立球面投影（底部极点向下俯视，非 FOV=180° 避免奇点）。
  通过 `g_asteroidWeight` uniform 控制投影模式插值（lerp 透视/小行星方向），复用同一 D3D11 管线（无第二渲染路径）。
  `AsteroidSchedule`（smoothstep 插值，首尾导数连续无突跳）提供 weight + 旋转起始帧。
  验证：关闭(透视) vs 启用(小行星) PSNR=10.24dB（不同投影）；过渡末 PSNR=100dB（无突跳）。
- **H.265 编码器探测**：`MfHevcEncoderProbe`（IHevcEncoderProbe 的 MF 实现，MFTEnumEx 枚举 HEVC 硬件编码器）。
  本机 NVENC HEVC 可用，`PresetResolver` 体积优先保留 H.265 无退回；不可用时退回 H.264 并提示原因。

## 附：C++ 原生 D3D11 探针（已验证环境，暂不用）
`src/PanoToVideo.Native/probe/main.cpp` 验证原生 C++ D3D11 在本环境同样可用
（Clear + 回读正常），作为下沉路径的环境验证保留。当前不进入下沉路径。
