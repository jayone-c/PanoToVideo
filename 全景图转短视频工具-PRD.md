# 全景图转短视频工具 PRD

- 状态：可直接交给开发 Agent
- 目标平台：Windows 10/11 x64
- 产品定位：将本地标准 2:1 等距柱状全景 JPG/PNG，批量转换为普通透视镜头 MP4；不是剪辑软件。
- 本期明确不做：下载目录监听/下载后自动入队、音乐、字幕、转场、滤镜、时间轴、多段剪辑、云端服务。

## Problem Statement

现有“全景图转短视频”工具能完成单图导出，却将全景投影、逐帧生成和 H.264 编码都压在 CPU 上；面对从全景下载器获得的 6000×3000、7680×3840、8192×4096 乃至更高分辨率图片，30 秒、1080×1920、60 FPS 的任务耗时过长，且没有批量生产能力。

用户需要保留现有单图参数模型，选择一张或多张本地 2:1 全景图后，以统一配置生成普通 MP4 短视频。默认路径必须优先使用 GPU 完成“全景投影 + H.264 编码”，而不能只是把最终编码器从 CPU 换成 NVENC。

## Solution

提供一个本地 Windows 桌面应用“全景图转短视频”。它将 2:1 等距柱状全景图作为球面贴图，以虚拟相机按线性水平旋转逐帧取出透视画面，输出无音频的 H.264 MP4。

应用保留截图中的所有参数：视频长度、旋转度数、帧速率、FOV、视频宽度、视频高度、CPU 核心、垂直视角、顺/逆时针、小行星开场、记住我的配置、完成后打开视频、单图生成。新增批量任务队列、硬件状态、输出目录/命名、进度、取消和失败重试。

### 成功标准

1. 在有可用独显的电脑上，默认使用 GPU 投影和硬件编码；界面明确显示实际使用的投影设备和编码器，不能只显示“GPU 加速”文案。
2. 在本机已确认可用的 RTX 4090 D（24GB）与 NVENC 环境中，标准基准任务（7680×3840 JPG → 1080×1920、30 秒、60 FPS、360°、75° FOV、H.264）端到端平均速度不得低于 60 输出 FPS；即 30 秒成片的纯渲染时间不超过 30 秒。该指标须以应用的实际日志验证。
3. 一次可添加至少 100 张图片；单个任务失败不阻塞队列；用户可重试失败项。
4. CPU/GPU 能力不足时，应用解释原因并提供 CPU 回退；不得伪称已启用硬件加速。
5. 输出视频可在 Windows 默认播放器和主流短视频剪辑软件中正常打开。

## User Stories

1. 作为全景图下载用户，我想拖入一张 2:1 全景图，以便生成单个 MP4。
2. 作为批量生产用户，我想一次选择多张全景图，以便用同一套参数批量导出。
3. 作为用户，我想看到每张图的缩略图、原始分辨率与校验结果，以便先发现非 2:1 或损坏文件。
4. 作为用户，我想设置视频长度（秒），以便控制成片时长。
5. 作为用户，我想设置旋转度数且允许大于 360°，以便控制相机总旋转圈数。
6. 作为用户，我想选择顺时针或逆时针，以便控制镜头方向。
7. 作为用户，我想设置帧速率，以便输出 30 FPS 或 60 FPS 的素材。
8. 作为用户，我想设置 FOV，以便控制画面取景广窄。
9. 作为用户，我想设置视频宽度和高度，以便生成 1080×1920 竖屏、1920×1080 横屏或自定义尺寸。
10. 作为用户，我想设置垂直视角，以便让镜头对准室内中部、上方或下方。
11. 作为用户，我想启用或关闭小行星开场，以便保留现有工具的开场效果。
12. 作为用户，我想保存常用参数，以便下次打开软件自动复用。
13. 作为用户，我想看到应用是否正在使用 NVIDIA、AMD、Intel 或 CPU，以便判断速度是否符合预期。
14. 作为用户，我想在硬件不可用时选择 CPU 核心数，以便控制 CPU 回退时对电脑的占用；GPU 路径中此项不应影响投影或编码。
15. 作为用户，我想选择输出目录和命名规则，以便找到批量结果且避免覆盖原图。
16. 作为用户，我想看到队列总进度、当前任务帧进度、已耗时、预计剩余时间和实际 FPS，以便判断任务是否正常。
17. 作为用户，我想暂停队列、取消当前任务和重试失败项，以便处理异常而不重新添加全部图片。
18. 作为用户，我想在全部任务完成后打开输出目录；单图任务可选择直接打开视频，以便快速检查结果。
19. 作为用户，我想获得易读的失败原因（非 2:1、无法读取、空间不足、编码器不可用、写入失败），以便自行处理。
20. 作为用户，我想在“无缝循环”条件满足时获得首尾同视角的结果；当旋转度数不是 360° 的整数倍时，应用应提示该视频不是无缝循环。

## Functional Requirements

### 输入与校验

- 支持拖放、文件选择和多选；支持 JPG、JPEG、PNG。
- 图片必须满足宽高比 `2.0 ± 1%`。不满足时不进入队列，并显示实际尺寸与比例。
- 支持读取 6000×3000 到至少 16384×8192 的 JPEG；不得先将原图静默降分辨率再投影。
- 读取失败、内存不足、显存不足、输出盘空间不足必须在入队或开跑前尽早报错。

### 单图参数（与现有界面一一对应）

| 现有控件 | 产品语义与规则 |
|---|---|
| 视频长度（秒） | 正整数，默认 30；决定总帧数 `时长 × FPS`。 |
| 旋转度数（度） | 默认 360；允许正整数，决定总水平位移。 |
| 帧速率（FPS） | 下拉：24、25、30、50、60；默认 60。 |
| 视场角（FOV） | 水平 FOV，30°–110°，默认 75°。垂直 FOV 由输出宽高比推导，避免两个 FOV 相互矛盾。 |
| 视频宽度/高度 | 必填偶数；预设为 1080×1920、1920×1080、1080×1080，亦可自定义。 |
| CPU 核心 | 仅“CPU 回退”模式显示并生效；默认自动。 |
| 垂直视角 | 实际语义为 Pitch，范围 -85° 至 85°，默认 0°；界面文案改为“俯仰角（°）”。 |
| 顺/逆时针 | 决定 Yaw 增量正负。 |
| 小行星开场 | 可选；默认关闭。固定为 0.8 秒，以 180° FOV/小行星投影过渡到用户 FOV，不提供时间轴编辑。 |
| 记住我的配置 | 保存最近一次有效配置到本机应用设置。 |
| 完成后打开视频 | 单图完成后打开该 MP4；批量完成时改为打开输出目录。 |

### 批量任务

- 单图和批量共用同一“参数面板”；多图只允许统一参数，不设计每条任务的剪辑编辑。
- 队列项包含：缩略图、源文件名、尺寸、状态、进度、实际 FPS、预计剩余时间、输出路径、错误原因。
- 状态机：`待校验 → 待处理 → 渲染中 → 编码中 → 完成`；异常进入`失败`，用户可重试；取消进入`已取消`。
- 排队策略：默认一次只运行一个 GPU 任务，防止多任务争抢显存和编码会话；CPU 回退任务亦默认串行。未来可评估并发，但不属于本期。
- 输出默认放入用户选择目录下的 `exports`；默认命名为 `{原文件名}_{宽}x{高}_{时长}s_{旋转度}deg.mp4`。有同名文件时追加递增序号，不覆盖。
- 导出前估算总帧数、预计输出体积和可用磁盘空间；不足时不开始。

### 输出

- 容器：MP4；默认 H.264、yuv420p、无音频、可播放优化（fast start）。
- 提供“兼容优先”和“体积优先”两个质量预设：前者 H.264，后者 H.265；若所选编码器不支持则提示并退回 H.264。
- 写入临时文件，只有编码成功后原子重命名为最终文件；失败或取消时清理临时文件，不删除源图。
- 记录每个任务的参数、设备、编码器、耗时、平均 FPS、输出文件和失败详情，供 UI 查看与排查。

## Implementation Decisions

### 核心技术决策

1. **Windows 原生 GPU 管线优先。** 使用 .NET 桌面 UI（WPF 或 WinUI 3）承载界面；渲染工作器使用 Direct3D 11 + HLSL。每帧在 GPU 将 equirectangular 图片按 Yaw、Pitch、FOV 映射为透视画面，再直接交给 Windows Media Foundation 的硬件 H.264 编码器。不得把每帧读回 CPU 后再上传给编码器；这会抵消 GPU 优势。
2. **跨 GPU 的设备选择。** 启动时枚举 DXGI 适配器与硬件编码器，默认选性能最高且可完成“GPU 投影 + 硬件 H.264”的设备。优先顺序为可用独显、其他可用硬件编码设备、CPU 回退。实际选择写入任务日志与界面。
3. **投影数学。** 对每个输出像素以透视相机射线求球面经纬度，再采样 2:1 ERP 纹理；Yaw 按 `起始 Yaw + 方向 × 旋转度 × 帧序号/(总帧数-1)` 计算，Pitch 固定为用户设置。横向采样必须在 0°/360° 接缝环绕，默认双线性插值。
4. **小行星开场独立实现。** 它是固定的投影过渡，不进入通用剪辑系统。普通视频从第 0 帧即使用透视投影；启用时前 0.8 秒执行小行星投影到透视投影的固定过渡，之后再按正常旋转导出。
5. **CPU 回退仅保障可用性。** GPU 初始化或硬件编码失败时，使用 FFmpeg `v360` + 软件编码/可用硬件编码的回退链路，并把“CPU 回退”显式标在任务状态中。GPU 投影失败时，不能在后台悄悄走 CPU。
6. **FFmpeg 的角色。** 当前环境的 FFmpeg 已验证存在 `v360`、`h264_nvenc`、`hevc_nvenc`、`h264_amf`、`hevc_amf`、`h264_qsv` 与 `hevc_qsv`。`v360` 可用于 CPU 回退和结果对照；编码器可用于兼容性兜底。主 GPU 路径不可只依赖 `v360 + h264_nvenc`，必须对投影阶段单独计时并验收。
7. **可复用开源组件的边界。**
   - 使用 [Pannellum](https://github.com/mpetroff/pannellum)（MIT）或等价 WebGL 实现做交互预览是可行的；它不承担视频导出。
   - 使用 [py360convert](https://github.com/sunset1995/py360convert)（MIT）作为投影数学的离线对照和测试基准；它是 Python/NumPy 路线，不能作为高性能生产渲染器。
   - 使用 [FFmpeg](https://github.com/FFmpeg/FFmpeg) 进行 CPU 回退、封装和验证；发布前必须核对实际构建的 LGPL/GPL 组件与随附许可。
   - [hvs_vr_encoding](https://github.com/horizon-research/hvs_vr_encoding) 含 CUDA 等距柱状投影示例及性能数据，但仓库未明确展示可复用许可证，禁止直接复制代码；只作为性能与算法调研参考。
   - 可参考 [Vortice.Windows](https://github.com/amerkoleci/Vortice.Windows) 的 MIT Direct3D 绑定与 [DirectX-Graphics-Samples](https://github.com/microsoft/DirectX-Graphics-Samples) 的 MIT 图形样例；若 .NET 绑定无法满足零拷贝编码，保留 C++/WinRT 工作器实现，不要为迁就 UI 技术牺牲 GPU 管线。
8. **不直接套用完整开源项目。** 本轮调研没有找到一个同时满足“静态 2:1 图片 → 可控透视旋转 MP4 → Windows GPU 投影 → 批量队列”的可直接 fork 成品。可复用的是预览、投影参考和媒体编解码组件；核心 GPU 导出管线需要独立实现。

### 界面结构

- 顶部：文件输入框、`添加图片`、`添加文件夹`、输出目录选择、硬件状态标签。
- 左侧：参数区，保留现有参数及三个输出预设；“CPU 核心”仅在 CPU 回退/高级设置中可编辑。
- 中部：队列列表；单图时也以一条任务呈现，避免两套流程。
- 右侧：当前任务详情、实时进度、实际 FPS、设备与编码器、`开始导出`、`暂停`、`取消`、`重试失败`。
- 底部：累计完成数、失败数、总进度、打开输出目录。

## Testing Decisions

### 测试原则

只验证用户可见结果和设备行为，不把测试绑死在某个类、着色器函数或控件结构上。所有 GPU 结果均需与可信 CPU 参考投影进行图像差异比对。

### 必测场景

1. 使用工作区已有的 6000×3000、7680×3840、8192×4096、16000×8000 标准 ERP 图片，验证均可入队和导出。
2. 对 Yaw 为 0°、90°、180°、270°，Pitch 为 -30°、0°、30°，FOV 为 45°、75°、100° 的单帧结果，与 py360convert 或 FFmpeg `v360` 参考图对比；接缝处不得出现黑边、断层或错误翻转。
3. 验证顺/逆时针在首帧一致、末帧朝向相反；360°整圈任务首尾视角一致，允许编码压缩差异但不允许几何跳变。
4. 验证 1080×1920、1920×1080、1080×1080 三种输出尺寸和 30/60 FPS 均能播放。
5. 验证小行星开场关闭时第 0 帧就是普通透视画面；启用时在 0.8 秒后无突跳进入普通透视旋转。
6. 验证批量 100 项：顺序、重名处理、单项失败后继续、失败重试、取消、临时文件清理、完成后打开目录。
7. 模拟并验证非 2:1、损坏图片、无输出写入权限、磁盘空间不足、GPU 初始化失败、硬件编码不可用等错误文案与回退行为。
8. 在 RTX 4090 D 基准机实测端到端 FPS、CPU 利用率、GPU 利用率、显存峰值和输出耗时；日志必须能证明投影与编码的实际设备。

### 验收门槛

- 所有功能测试通过；输出 MP4 由 ffprobe 校验为视频流、正确分辨率、正确帧率、正确时长、无音频流。
- 基准机达到“成功标准”第 2 条；若不达标，不得把 GPU 版本标记为完成，应先在日志中定位投影、GPU→CPU 回读或编码哪一阶段成为瓶颈。
- GPU 不可用时 CPU 回退功能可完成至少一张图，且 UI/日志明确说明回退原因。

## Out of Scope

- 全景图下载完成后自动监控、自动入队、去重或项目管理。
- 视频剪辑、配乐、字幕、滤镜、转场、复杂镜头路径、时间轴。
- 多机分布式渲染、云端队列、账号体系、分享/发布。
- AI 自动找主体、自动推荐取景。
- 3D 深度估计、自由移动相机、补画面、AI 视频生成。
- 360 视频输入、立体全景、非标准投影格式输入。

## Further Notes

- “视频宽度/高度”与 FOV 共存时，FOV 必须定义为水平 FOV；这是避免截图中“FOV”和“垂直视角”语义混乱的必要修正。
- 当前机器已经实测 NVENC 可用；因此开发第一阶段应先完成一个端到端 GPU 冒烟样例：一张本地 ERP 图片生成 3 秒、1080×1920、60 FPS H.264，再接入完整 UI 和队列。不能先做完整界面、最后才验证 GPU 管线。
- `v360` 的官方文档确认支持 equirectangular 输入、regular/perspective 输出、FOV 与输出分辨率；它适合作为参考/回退，但本产品性能目标要求单独验证投影阶段是否仍占 CPU。
- 工作区现有全景下载器产物已是标准 `*_equirectangular_<width>x<height>.jpg`，这正是本软件的输入契约；不处理 six-face 原图或普通截图。
- 本工作区不是 Git 仓库，也未配置 issue tracker；本 PRD保存为本地交付物，未发布到外部任务系统。

## Agent Delivery Order

1. 搭建最小端到端 GPU 冒烟路径并测量投影、编码、总耗时。
2. 完成 ERP 校验、单图参数、GPU/CPU 设备探测和单图导出。
3. 接入队列状态机、批量任务、进度、取消、重试、命名和日志。
4. 实现配置记忆、完成后打开、输出预设和小行星开场。
5. 执行几何对照、播放兼容性、错误恢复与 RTX 4090 D 性能验收；达标后再打包发布。
