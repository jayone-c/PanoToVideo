<UserControl x:Class="PanoToVideo.App.Controls.NumericStepper"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             Height="30">
    <Border Background="{DynamicResource SurfaceAltBrush}"
            BorderBrush="{DynamicResource StrokeBrush}"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="26"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NumericTextBox"
                     Text="{Binding Value, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                     BorderThickness="0"
                     Background="Transparent"
                     Margin="0"
                     Padding="8,3"
                     VerticalContentAlignment="Center"
                     PreviewTextInput="NumericTextBox_PreviewTextInput"
                     LostKeyboardFocus="NumericTextBox_LostKeyboardFocus"
                     KeyDown="NumericTextBox_KeyDown"/>
            <StackPanel x:Name="ButtonsPanel" Grid.Column="1" Margin="0,2,2,2">
                <Button x:Name="IncreaseButton" Content="▲" Click="Increase_Click"
                        FontSize="8" FontWeight="Normal" Padding="0" Height="12"
                        Style="{StaticResource QuietButton}"/>
                <Button x:Name="DecreaseButton" Content="▼" Click="Decrease_Click"
                        FontSize="8" FontWeight="Normal" Padding="0" Height="12" Margin="0,1,0,0"
                        Style="{StaticResource QuietButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
